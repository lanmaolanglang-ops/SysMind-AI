# SysMind AI v0.1.0-preview 用户指南

SysMind AI 用本机只读证据帮助解释 Windows 电脑问题。它不是聊天机器人，也不会让模型执行
Shell、PowerShell、任意命令或自动修改系统。

## 开始一次诊断

1. 打开应用，等待顶部显示“本地服务已连接”。
2. 保持在“解决电脑问题”页面。
3. 用自己的话描述现象，例如：
   - 电脑最近很卡
   - 游戏突然掉帧
   - 软件一直闪退
   - 无法联网
4. 选择“开始诊断”。网络问题会访问少量固定测试地址，其他诊断默认只读取本机状态。
5. 查看正在执行的检查；需要时可以取消。
6. 如果系统要求补充发生场景、应用名称或时间，请在原诊断中回答，已有检查不会重复执行。

## 阅读诊断报告

报告首先展示：

- 问题总结和可信程度
- 发现的问题
- 本机证据来源、检查时间和关键指标
- 可能原因、支持证据和反证
- 建议下一步
- 当前报告限制

“证据不足”表示现有检查无法确定原因，不代表电脑没有问题。建议在问题正在发生时重新诊断，
补充更具体的应用名称和发生时间，或保存易读报告交给可信的技术人员。

工具名称、调用 ID 和字段路径属于技术审计信息，默认位于展开区域，普通使用不需要理解这些字段。

## 本地模式与远程 Provider

不配置远程 Provider 时，计划和报告使用本地确定性规则，可以离线工作。配置远程 Provider 是
可选操作，可能发送脱敏后的用户问题、最小设备摘要、只读工具说明以及有界观察摘要。

不会发送原始日志、完整事件 XML、API Key、用户文件内容或任何系统修改权限。完整范围见
[隐私说明](docs/privacy.md)。不要在问题描述中输入密码、API Key 或不必要的个人信息。

## 历史、导出与删除

- “记录与报告”可重新查看已保存的诊断。
- 报告可以导出为脱敏 Markdown 或 JSON；导出后的文件由用户自行保管。
- 默认保留期限为 30 天，可在“高级工具 → 模型与隐私”中调整并立即清理。
- 与受控操作关联的审计和恢复记录不会被普通历史清理删除。
- 卸载默认保留本地数据，详见 [安装说明](INSTALL.md#升级与卸载)。

## 安全边界

诊断 Agent 只能选择应用 Registry 中已存在的版本化只读工具。少量既有受控操作位于独立流程，
必须由用户逐项确认，并且不会由模型自动执行。SysMind AI 不提供服务修改、网络配置修改、任意
注册表写入、文件删除或自动修复。

使用前请阅读 [当前限制](LIMITATIONS.md)。
