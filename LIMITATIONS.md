# SysMind AI v0.1.0-preview 当前限制

本文件描述预览版已经确认的产品和验证边界。遇到证据不足时，SysMind AI 应明确说明无法确定
原因，而不是依靠经验猜测。

## 支持范围

- 仅支持 Windows 10/11 x64 当前用户安装。
- RC2 只在一台 Windows 11 x64 真实设备上完成单机验证。
- 尚未完成不同硬件、Windows 10、企业组策略、域环境和受限账户的兼容矩阵。
- 当前安装包未进行 Windows 代码签名，不能作为生产签名版本发布。

## 诊断限制

- 诊断主要依据开始任务时的短时快照。间歇性卡顿、偶发掉帧和较早发生的崩溃可能需要在问题
  复现时重新检查。
- GPU 检查提供显卡和驱动元数据，并通过 WDDM 性能计数器采样一次实时利用率与已用专用显存。
  计数器按 LUID 索引，而 `Win32_VideoController` 不暴露 LUID，因此**无法按显卡归因**：多显卡设备上
  该读数是系统级的（利用率取最忙的那一块，显存取全部之和），只挂在第一个显卡条目上。部分驱动或
  远程会话不暴露这些计数器，此时该字段回落为不可用。
- 事件日志仅查询 Application 和 System 白名单频道、限定时间窗和限定记录数；权限不足时会
  保留其他证据并降低结论可信度。记录开机/关机/待机性能的
  `Microsoft-Windows-Diagnostics-Performance/Operational` 未纳入白名单：实测该频道需要提权
  才能枚举和读取，与"当前用户、非提权"的运行方式冲突，加入后也取不到数据。
- 网络诊断只访问应用固定允许的 DNS/ICMP 目标。ICMP 失败本身不能证明整个互联网不可用。
- 启动项数量、当前高占用进程或无关崩溃事件只能作为相关证据，不能单独证明用户描述问题的
  原因。
- Fake Planner 使用确定性关键词保证离线可用，并不具备远程语言模型的全部语义理解能力。
- 配置远程 Provider 后，规划与 Agent 两条路径都要求模型按约定返回结构化 JSON。返回纯文本或
  畸形结构的模型会以协议错误结束该轮，SysMind AI 不会把这段文本当作最终答案。离线本地模式
  不受此限制；自行接入兼容性不明的模型端点时可能更容易触发该错误。
- 本地规则阈值尚未经过大规模真实用户样本校准；置信度表示当前证据强度，不是统计因果概率。

## 运行与数据限制

- 单项工具失败不会使整个任务失败，但报告可能标记为“部分完成”或“证据不足”。
- 被应用重启中断的**扫描与日志分析会自动重放**：启动后 15 分钟内开始、且因重启而中止的任务会被
  重新执行（每次启动最多 2 个），用户无需重新发起。更早的任务仍保持失败状态 —— 它采集的机器状态
  已经过时，重放会把上周的证据当成当前情况呈现。等待用户补充信息的任务可在重启后继续。
- 本地数据默认保存在 `%LOCALAPPDATA%\ai.sysmind.desktop`。静默卸载保留数据；交互式主动
  删除尚未在一次性虚拟机中完成 RC2 验证。
- 数据库迁移脚本包含 upgrade 与 downgrade，且 `test_database` 覆盖阶段 5→head 的降级/升级往返；产品与运维策略仍只支持向前恢复（roll-forward），正式升级失败时请用前向修复而不是降级。
- 当前没有强制遥测、云端账号、多用户、插件市场或跨设备同步。
- 桌面端 CSP 的 `connect-src` 对 `http://127.0.0.1` 全端口放行。后端每次启动监听随机回环
  端口并通过 sidecar 握手告知前端，静态 CSP 无法枚举该端口，因此这是当前架构下的既定取舍。
  收紧需要让前端改走 Tauri IPC（由 Rust 侧代理后端请求），属于架构变更。

## 正式发布尚缺少的验证

- Windows Authenticode 和 Tauri updater 正式签名
- 干净 Windows 10/11 虚拟机安装、升级、交互式卸载和异常恢复矩阵
- 多硬件与受限权限环境验证
- 安装包和解包二进制的杀毒软件/多引擎扫描
- 更广泛的真实用户可用性复核和诊断阈值校准

详细现场记录见 [RC2 Local Validation](docs/progress/release-candidate-2-local-validation-complete.md)。
